<?xml version="1.0" encoding="UTF-8" ?>
<!-- mybatis 맵퍼 관련 자동완성이 나오도록 하는 설정 -->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.company.mappers.MemberMapper">
    <select id="login" resultType="org.zerock.company.vo.MemberVO">
        SELECT
            id, name, pass, checkpass
        FROM members
        WHERE id = #{id} AND pass = #{pass}
    </select>
    <insert id="join" >
        INSERT INTO members (id, name, pass, checkpass)
        VALUES (#{id}, #{name}, #{pass}, #{checkpass})
    </insert>
</mapper>